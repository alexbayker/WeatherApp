<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:id="@+id/downloadinglayout"
    android:background="@color/white"
    android:layout_gravity="bottom">

    <RelativeLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/subdownloadinglayout"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        tools:ignore="UselessParent">

        <ProgressBar
            android:id="@+id/downloadingbar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerVertical="true"
            android:layout_marginEnd="@dimen/defaultpadding"
            android:indeterminate="true"
            android:indeterminateTint="@color/black"
            android:indeterminateTintMode="src_atop" />

        <TextView
            android:id="@+id/downloadingtitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/defaultpadding"
            android:layout_alignParentTop="true"
            android:layout_toEndOf="@id/downloadingbar"
            android:background="@color/colortransparent"
            android:gravity="center"
            android:maxLines="1"
            android:paddingVertical="@dimen/defaultpadding"
            android:scrollbars="none"
            android:scrollHorizontally="false"
            android:singleLine="true"
            android:text="@string/cities_loading"
            android:textColor="@color/black"
            android:textSize="@dimen/citysearchtextsize" />

    </RelativeLayout>

</RelativeLayout>